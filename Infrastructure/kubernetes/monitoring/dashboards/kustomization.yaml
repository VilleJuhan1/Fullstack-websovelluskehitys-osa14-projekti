apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

configMapGenerator:
  - name: custom-grafana-dashboards
    files:
      - 11670_rev4.json
      - 1860_rev45.json
    options:
      labels:
        grafana_dashboard: "1"
