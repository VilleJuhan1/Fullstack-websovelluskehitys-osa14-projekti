apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - backend.yaml
  - frontend.yaml
  - postgres.yaml
  - network-policy.yaml
  - ingress.yaml # Disabled until Nginx Ingress is properly deployed 
  - frontpage.yaml
  - stripe-mock.yaml

images:
  - name: villejuhan1/quiz-backend
    newTag: stable
  - name: villejuhan1/quiz-frontend
    newTag: stable
  - name: villejuhan1/frontpage
    newTag: stable
