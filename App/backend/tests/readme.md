# Backend tests

E2E tests are run at every build workflow and can also be manually run by using command `npm run test:e2e`. Rest tests are used for manual testing while developing. Tests use the same migrations and database as the development instance.