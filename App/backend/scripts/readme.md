# Backend scripts

## Initialize the seed data

The functions in dataBuilder.py are used to build the initial datasets for backend in JSON format. The are then used to seed the database via migrations. Having the data locally at first makes developing easier.