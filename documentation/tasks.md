# Tasks

## April
- [ ] Create a working backend that returns a compiled list of json objects. The data will be fetched from another API. The backend should be an apollo server (graphql) written in typescript. This is the MVP backend with no user functionalities etc.
- [ ] Create a working frontend for the application which presents a flag and four options for the user to choose from. The user should be informed if they chose the correct country for the flag.